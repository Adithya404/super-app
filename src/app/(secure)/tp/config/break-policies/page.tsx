// import { getSessionUser } from "@/lib/auth/get-session";

export default async function BreakPolicies() {
  // const session = await getSessionUser();
  // console.log("session", session);
  return <div>BreakPolicies</div>;
}
